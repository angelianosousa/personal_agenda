<!-- Modal -->
<div class="modal fade" id="taskModal_<%= board.id %>" tabindex="-1" role="dialog" aria-labelledby="taskModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg modal-dialog-scrollable" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="taskModalLabel">Quadro: <%= board.title %></h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>

      <div class="modal-body">
        <div class="card-body">

          <div class="title-left">
            <%= link_to " ", edit_users_backoffice_board_path(board), class:"btn btn-success btn-round fa fa-plus-square", style:"float:right;width:100px;font-size:15px;" %>
            <h3>  Últimas atividades adicionadas </h3>
            <h6>Quantidade: <%= board.tasks_count %></h6>
          </div>

          <% board.tasks.each do |task| %>

            <div class="alert alert-secondary" style="">
              <p>
                <%= link_to " ", users_backoffice_task_path(task), method: :delete, data: {confirm: "Apagar #{task.title} ?"}, class:"btn btn-danger fa fa-trash", style:"float:right;" %>
                <%= link_to " ", edit_users_backoffice_task_path(task), class:"btn btn-secondary fa fa-edit", style:"float:right;" %>
                <h5 class="card-title"><%= task.title.truncate(20) %></h5>
                <strong>Data de Entrega: </strong><p class="<%= task_style(task) %>"> <%= I18n.l(task.start_time) %></p>
                <p><%= task.description %></p>
              </p>
            </div>

          <% end %>
        </div>
      </div>

    </div>
  </div>
</div>