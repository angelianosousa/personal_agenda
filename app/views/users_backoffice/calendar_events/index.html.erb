  <div class="x_content">
    <div id="calendar">
      <div class="simple-calendar" style="diplay:inline-block;">
        <%= month_calendar(events: @tasks) do |date, tasks| %>
          <%= I18n.l(date) %>
          
          <% tasks.first(2).each do |task| %>
            <br>
            <label class="badge badge-primary" data-toggle="modal" data-target="#taskModal_<%= task.board.id %>" title="Clique para ver todos os tópicos desse quadro">
              <%= task.board.title.truncate(20) %>
            </label>

            <%= render partial: '/users_backoffice/boards/shared/tasks_modal', locals: { board: task.board } %>

          <% end %>

        <% end %>
      </div>
    </div>

  </div>

</div>
